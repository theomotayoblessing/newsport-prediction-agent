# REQUIRED for deployment
# Use a Postgres database (Supabase or Neon). Example:
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# Use live APIs (recommended). If true, app runs without any keys (demo mode).
USE_MOCK_DATA="true"

# API-FOOTBALL (optional)
APIFOOTBALL_KEY=""
APIFOOTBALL_BASE="https://v3.football.api-sports.io"

# The Odds API (optional)
ODDSAPI_KEY=""
ODDSAPI_BASE="https://api.the-odds-api.com/v4"

# League IDs (API-Football). Default: EPL, LaLiga, Serie A, Bundesliga, Ligue 1
DEFAULT_LEAGUES="39,140,135,78,61"

# If deploying (Vercel), set NEXT_PUBLIC_BASE_URL to your deployed URL (optional)
NEXT_PUBLIC_BASE_URL=""
